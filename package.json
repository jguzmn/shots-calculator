{
  "name": "shots-calculator",
  "version": "0.1.0",
  "private": true,
  "description": "Calculadora de tragos con API Node.js y base de datos Neon.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check scripts/seed-botellas.js && node --check scripts/import-catalogo-base.js && node --check scripts/seed-demo-user.js",
    "seed": "node scripts/seed-botellas.js",
    "seed:demo-user": "node scripts/seed-demo-user.js",
    "import:catalogo-base": "node scripts/import-catalogo-base.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
